var target= 0 ;
var cargarPagina = function(){
	var $botones = (".control");
	console.log($botones);
	botones.each(function(boton,indice){
		boton.addEventListener
	})
}

window.addEventListener("load", cargarPagina);